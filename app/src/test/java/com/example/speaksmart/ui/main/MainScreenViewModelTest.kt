package com.example.speaksmart.ui.main

import org.junit.Test

class MainScreenViewModelTest {
    @Test
    fun uiState_initialState_isDefault() {
        // ViewModel requires Application context, so this is a placeholder.
        // For a real test, use Robolectric or AndroidJUnit4.
        assert(true)
    }
}
