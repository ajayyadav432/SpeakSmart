package com.crispr.ai.theme

import androidx.compose.ui.graphics.Color

// ─── PRIMARY ACCENT — Teal Glow #00E5C8 ───────────────────────────
val PrimaryDark         = Color(0xFF00E5C8)   // main teal glow
val PrimaryLight        = Color(0xFF00FFC8)   // bright mint highlight
val PrimaryContainer    = Color(0xFF003D35)   // teal container bg
val OnPrimaryContainer  = Color(0xFFB2FFF4)   // text on teal container

// ─── BACKGROUND & SURFACES — Dark charcoal ────────────────────────
val SurfaceDark         = Color(0xFF141A1E)   // deepest bg (dark forest charcoal)
val SurfaceVariantDark  = Color(0xFF1A2228)   // slightly lighter card bg
val SurfaceElevatedDark = Color(0xFF202B32)   // elevated surfaces, input bar
val SurfaceCardDark     = Color(0xFF1E2A30)   // cards
val SurfaceNavDark      = Color(0xFF151D22)   // bottom nav bar

// ─── TEXT ─────────────────────────────────────────────────────────
val OnSurfaceDark       = Color(0xFFE8F0EF)   // primary text (warm white)
val OnSurfaceVariantDark= Color(0xFF7A9A94)   // secondary text (muted teal-grey)
val OnSurfaceMutedDark  = Color(0xFF3D5550)   // placeholder / disabled

// ─── STATUS ───────────────────────────────────────────────────────
val SuccessGreen        = Color(0xFF00E5C8)
val ErrorRed            = Color(0xFFFF6B6B)
val WarningAmber        = Color(0xFFFFB74D)

// ─── MIC BUTTON ───────────────────────────────────────────────────
val MicButtonStart          = Color(0xFF00E5C8)
val MicButtonEnd            = Color(0xFF00FFDA)
val MicButtonPressedStart   = Color(0xFFFF6B6B)
val MicButtonPressedEnd     = Color(0xFFFF8A65)
val MicButtonGlow           = Color(0x4000E5C8)
val MicButtonPressedGlow    = Color(0x40FF6B6B)

// ─── AGENT ACCENT COLORS (vivid, per-agent) ───────────────────────
val AgentTeal           = Color(0xFF00E5C8)   // English Coach
val AgentPurple         = Color(0xFFB67CFF)   // Deep Reflection
val AgentAmber          = Color(0xFFFFB74D)   // Deal Coach
val AgentGreen          = Color(0xFF3EE68C)   // Financial Auditor
val AgentBlue           = Color(0xFF58A6FF)   // Second Brain
val AgentOrange         = Color(0xFFFF8C42)   // Travel
val AgentRed            = Color(0xFFFF6B6B)   // Medical

// ─── SECTION COLORS ───────────────────────────────────────────────
val SpeechSectionColor      = Color(0xFF58A6FF)
val CorrectionSectionColor  = Color(0xFFB67CFF)

// ─── SECONDARY ────────────────────────────────────────────────────
val SecondaryDark = Color(0xFFFFB74D)
val SecondaryLight = Color(0xFFF57C00)
